-- ================================================================
-- Режим "Призрак" для экзекьюторов (Delta, Synapse, Krnl и др.)
-- Переключение по клавише G (можно сменить)
-- ================================================================

local player = game.Players.LocalPlayer
local character = player.Character or player.CharacterAdded:Wait()
local UserInputService = game:GetService("UserInputService")

local isGhost = false  -- true = режим включён

-- ===== 1. Блокировка CanTouch (персонаж не активирует триггеры) =====
local function setCanTouch(state)
    for _, part in ipairs(character:GetDescendants()) do
        if part:IsA("BasePart") then
            part.CanTouch = state
        end
    end
end

-- Обновляем при смене персонажа
player.CharacterAdded:Connect(function(newChar)
    character = newChar
    if isGhost then setCanTouch(false) end
end)

-- ===== 2. Блокировка инструментов (Enabled = false) =====
local function setToolsEnabled(state)
    for _, tool in ipairs(player.Backpack:GetChildren()) do
        if tool:IsA("Tool") then
            tool.Enabled = state
        end
    end
    for _, tool in ipairs(character:GetChildren()) do
        if tool:IsA("Tool") then
            tool.Enabled = state
        end
    end
end

player.Backpack.ChildAdded:Connect(function(tool)
    if tool:IsA("Tool") and isGhost then tool.Enabled = false end
end)
character.ChildAdded:Connect(function(tool)
    if tool:IsA("Tool") and isGhost then tool.Enabled = false end
end)

-- ===== 3. Блокировка ClickDetectors =====
local function setClickDetectorsEnabled(state)
    for _, detector in ipairs(workspace:GetDescendants()) do
        if detector:IsA("ClickDetector") then
            detector.Enabled = state
        end
    end
end

workspace.DescendantAdded:Connect(function(obj)
    if obj:IsA("ClickDetector") and isGhost then
        obj.Enabled = false
    end
end)

-- ===== 4. Блокировка RemoteEvent / RemoteFunction =====
local originals = {}
local function patchRemote(obj)
    if originals[obj] then return end
    if obj:IsA("RemoteEvent") then
        local oldFire = obj.FireServer
        originals[obj] = oldFire
        obj.FireServer = function(self, ...)
            if isGhost then return end
            return oldFire(self, ...)
        end
    elseif obj:IsA("RemoteFunction") then
        local oldInvoke = obj.InvokeServer
        originals[obj] = oldInvoke
        obj.InvokeServer = function(self, ...)
            if isGhost then return end
            return oldInvoke(self, ...)
        end
    end
end

-- Патчим уже существующие объекты
for _, obj in ipairs(game:GetDescendants()) do
    patchRemote(obj)
end
-- Патчим новые
game.DescendantAdded:Connect(patchRemote)

-- ===== 5. Функция переключения =====
local function toggleGhost()
    isGhost = not isGhost
    setCanTouch(not isGhost)
    setToolsEnabled(not isGhost)
    setClickDetectorsEnabled(not isGhost)
    -- Уведомление в консоль
    print("Режим призрака:", isGhost and "ВКЛЮЧЁН" or "ВЫКЛЮЧЁН")
end

-- ===== 6. Привязка к клавише G =====
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.G then
        toggleGhost()
    end
end)

-- Инициализация (режим выключен)
setCanTouch(true)
setToolsEnabled(true)
setClickDetectorsEnabled(true)
print("Скрипт загружен. Нажмите G для переключения режима призрака.")
